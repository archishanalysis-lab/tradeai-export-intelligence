# TradeAI End-to-End Workflow Testing

The full workflow checklist lives in `docs/TESTING_WORKFLOWS.md`.

Use that document to test exporter onboarding, product upload, AI buyer matching,
inquiry status changes, analytics refresh, file uploads, organization boundaries,
and staging smoke tests.
